apply plugin: "com.android.application"
apply plugin: "org.jetbrains.kotlin.android"

import com.android.build.OutputFile

android {
    namespace "com.appmosafir"
    compileSdk = 34

    defaultConfig {
        applicationId "com.appmosafir"
        minSdk = 21
        targetSdk = 34
        versionCode 1
        versionName "1.0"
        multiDexEnabled true

        // Ensures compatibility with libraries that use missing dimensions
        missingDimensionStrategy 'react-native-camera', 'general'
    }

    buildTypes {
        release {
            minifyEnabled false
            proguardFiles getDefaultProguardFile("proguard-android-optimize.txt"), "proguard-rules.pro"
        }
    }

    packagingOptions {
        pickFirst "**/*.so"
    }

    buildFeatures {
        viewBinding true
    }
}

dependencies {
    // React Native core and Hermes engine
    implementation "com.facebook.react:react-android:0.73.4"
    implementation "com.facebook.react:hermes-android:0.73.4"

    implementation "androidx.appcompat:appcompat:1.6.1"
    implementation "com.google.android.material:material:1.10.0"
    implementation "androidx.swiperefreshlayout:swiperefreshlayout:1.1.0"
    implementation "org.jetbrains.kotlin:kotlin-stdlib:1.9.10"
    implementation "androidx.multidex:multidex:2.0.1"
}
